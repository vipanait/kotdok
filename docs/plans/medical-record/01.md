# MR-01. Основа медкарты и переходы

> Для исполняющего агента: прочитать [общий план](README.md), исходную спецификацию и выполнить только этот этап через `superpowers:executing-plans`. Следующий этап не начинается автоматически.

**Цель:** Медкарта открывается из списка питомцев и показывает существующие данные анкеты. Старое редактирование доступно по «Анкета».

**Зависит от:** Нет.

**Макеты:** М1–М3, М27, X-loading, X-load-error.

**Архитектура:** вертикальный этап: контракт → сервер/хранение → мобильный UI → проверки. Новые серверные маршруты входят в `/api/v1`; UI использует общий API-клиент.

**Стек:** текущие Expo/React Native, Next.js API, PostgreSQL, Zod, Vitest. Версии и инструкции проверяются в репозитории перед кодом.

## Файлы и границы

- `apps/mobile/app/(tabs)/pets/[id]/index.tsx`
- `apps/mobile/app/(tabs)/pets/[id]/edit.tsx (новый)`
- `apps/mobile/app/(tabs)/pets/index.tsx`
- `apps/mobile/app/(tabs)/pets/new.tsx`
- `packages/contracts/src/medical-record.ts (новый)`
- `apps/web/src/app/(backend)/api/v1/pets/[id]/health/route.ts (новый)`
- `apps/web/src/server/medical-record/overview-service.ts (новый)`

Новые пути — предлагаемая структура. Если эквивалент уже появился, расширить его и зафиксировать фактический путь, не создавать дубль. Для миграции `<новая_метка>` заменить на следующую свободную метку времени репозитория.

## Шаги

- [ ] Зафиксировать базовые проверки и убедиться, что зависимости этапа приняты, а не просто начаты.
- [ ] Создать тестовые сценарии по MR-01.1…4 ниже; для логики данных сначала воспроизвести отсутствие/ошибку поведения на минимальном тесте.
- [ ] Снять исходное поведение списка, создания/редактирования, удаления питомца и проверки симптомов. Зафиксировать исходные проверки в отчёте этапа.
- [ ] Добавить контракт HealthOverview: pet_id, данные анкеты, доступные разделы и пустые массивы записей; nullable-значения не заменять ложным «нет». Добавить авторизованный GET /api/v1/pets/{id}/health.
- [ ] Перенести текущую форму [id]/index.tsx в [id]/edit.tsx, сохранив обработчики, поля, валидацию и защиту несохранённых изменений. На index.tsx разместить медкарту. Исправить переход после создания и историю питомца.
- [ ] Собрать шапку, «Важно знать» и разделы в существующих Screen/Card/Button. Для старых данных показать вес, статус вакцинации и препараты из анкеты, не создавать фиктивные даты событий.
- [ ] Недоступные до следующих этапов операции скрыть через единый список доступных возможностей. Не показывать неработающую кнопку добавления. Баннер первой прививки включить с этапа 02.
- [ ] Обработать загрузку, недоступность сети и ошибку с сохранёнными данными. Не кэшировать чужого питомца после смены аккаунта.
- [ ] Запустить адресные тесты, проверки типов и регрессию затронутых старых сценариев. Проверки SQL-транзакций/RLS выполнить на изолированной тестовой базе, не ограничиваться моками.
- [ ] Сверить UI с указанными артбордами; записать доказательства для каждого критерия в `docs/verification/medical-record-01.md`. Отдельно записать то, что проверить не удалось.
- [ ] Представить рабочий результат этапа на приёмку. Незакрытые критерии означают, что этап не завершён.

## Нумерованные критерии приёмки

- [ ] **MR-01.1** Свой питомец → медкарта → анкета → сохранение → медкарта: поля не теряются.
- [ ] **MR-01.2** Чужой pet_id, удалённый питомец и неактивный аккаунт не возвращают медданные.
- [ ] **MR-01.3** Бобик показывает 28 кг и «Привит(а), даты не указаны»; пустые аллергии не отображаются как «аллергий нет».
- [ ] **MR-01.4** Новый питомец после сохранения открывается на собственной медкарте; существующие проверки и история работают.

## Тесты этого этапа

Предлагаемые новые файлы: `apps/web/tests/server/medical-record/stage-01.test.ts`, `apps/web/tests/integration/medical-record-01.test.ts`, `apps/mobile/src/features/medical-record/stage-01.test.ts`. Создавать только относящиеся к этапу; чистый UI не требует фиктивного серверного теста. Нативные сценарии фиксируются отдельно с устройством/версией ОС.

Команды запуска и шаблон отчёта — в общем плане. Для MR-01 ожидается успешное выполнение каждого критерия выше, а не только зелёный общий набор тестов.
