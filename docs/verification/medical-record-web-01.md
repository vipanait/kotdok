# MW-01: основа и медкарта (веб)

Статус: **закрыт**. Все пять критериев проверены на реальном API. Оговорки и непроверенное — в конце.
Сборка: ветка `feature/medical-record-web`, код — коммит `28f0382`. Не отправлена и не влита. Дата: 26 сентября 2026.

Окружение:
- локальный Supabase (CLI 2.115, `127.0.0.1:54321`);
- сайт — `next dev -p 3100` (Next.js 16.3.4) с переменными из `apps/web/.env.integration` (в `.claude/launch.json` конфигурация `web-local`);
- браузер — Chrome через Playwright 1.62, контексты 1440×1000 и 390×844, `ru-RU`, `Europe/Moscow`.

Владельцы — фикстуры интеграционного набора:
- `owner-a@fixture.local` — хозяин Мурки и Бобика;
- `owner-b@fixture.local` — чужой владелец для проверки доступа.

Демо-данные создаёт [`apps/web/scripts/seed-medical-record-demo.mjs`](../../apps/web/scripts/seed-medical-record-demo.mjs):
- **Мурка** — записи внесены через API v1: три взвешивания, прививки с планом на 12.03.2027, две обработки, два визита, два курса. Прямо в базу записаны только две вещи, которых API не создаёт: проверка симптомов от 1 августа (её делает анализ) и план Бравекто на 12 сентября (API не принимает план в прошлом, а в жизни он создан в июне).
- **Бобик** — старый аккаунт: строка `pets` без медкарты. Вес 28 кг без даты, «привит» только в анкете.

План: [01.md](../plans/medical-record-web/01.md). Автоматический прогон приёмки: [`medical-record-web-01/verify.mjs`](medical-record-web-01/verify.mjs), вывод — [`verify-output.json`](medical-record-web-01/verify-output.json).

## Что сделано

- `/pets/[id]` — медкарта. Сервер проверяет, что питомец принадлежит вошедшему, через `getPet`: чужой, удалённый и неверный id дают 404 ещё до отрисовки. Затем страница читает `getHealthOverview` и последние три проверки питомца (`listChecks`) общим API-клиентом `@lapka/shared`.
- Токен Bearer берётся из сессии Supabase самого сайта (`features/api/browser-api.ts`). Хранилище токенов мобильного приложения не копируется.
- Отдельный `getPet` в браузере не вызывается: по контракту `overview.pet` — это ровно ответ `GET /pets/{id}`, второй запрос дублировал бы его.
- Состав страницы — по макету `medical` / `medical-empty`:
  - шапка: вид, порода, возраст, стерилизация, вес с трендом;
  - «Сроки»: первые три, остальные посчитаны;
  - «Важно знать»;
  - четыре раздела карточками по две строки;
  - вес: график и текстовая альтернатива;
  - «История проверок»: карточка — целиком ссылка-блок.
- Правила сроков, веса и курсов в календарных днях вынесены в portable-модуль `packages/shared/src/medical-record/record-overview.ts`. Тексты — в словарях веба (ru/en). `today` — день по часам браузера владельца.
- Состояния — `record-load.ts`, чистый редьюсер:
  - загрузка — скелетон макета `loading`;
  - первая загрузка не удалась — ошибка с «Повторить», медкарта не рисуется;
  - обновление не удалось — прежние данные и баннер макета `load-error`;
  - 404 — «Медкарта недоступна»;
  - 401 — «Сессия закончилась»: данные и кэш сбрасываются;
  - аккаунт в удалении — переход на `/account-deletion`.

  Кэш — только в памяти вкладки, в хранилище браузера ничего не пишется.
- Флаг этапов — один объект `MEDICAL_RECORD_STAGE` в `features/medical-record/stage.ts`. Раздел открыт, только когда его включил этап и сервер указал его в `writable`. На MW-01 всё выключено. Поэтому скрыты:
  - «Добавить запись» и «Для врача»;
  - «Все →» у разделов;
  - «Сделано» и «Все сроки».

  Вместо «Все сроки» — текст «Ещё N сроков». Там же маршруты следующих этапов (`medicalRecordHref`).
- Список питомцев и обзор ведут на `/pets/[id]`. Анкета `/pets/[id]/edit` не изменилась, только её ссылка «назад» ведёт в медкарту.

## Критерии

| Критерий | Результат | Доказательство |
|---|---|---|
| MW-01.1 Владелец открывает свою медкарту по прямой ссылке и после перезагрузки | PASS | `verify.mjs`: `murkaDirect` и `murkaAfterReload` = «Мурка» после `page.goto` на прямой URL и `page.reload()`. Ссылки из списка ведут на медкарту: `petListLinks` → `/pets/<id>`. Скриншоты: [murka-1440](medical-record-web-01/murka-1440.png), [pets-1440](medical-record-web-01/pets-1440.png). |
| MW-01.2 Чужой petId и выход из аккаунта не раскрывают данные (UI и API) | PASS | **Чужой.** Владелец B открывает `/pets/<Мурка>` — страница 404 ([foreign-1440](medical-record-web-01/foreign-1440.png)). Браузер не делает ни одного запроса к `/api/v1` (`apiRequests: []`), в HTML нет данных медкарты (`leaksRecord: []`). API под токеном B: `GET /pets/<Мурка>/health` → 404 `not_found`; `GET /checks?pet_id=<Мурка>` → пустой список. **Выход.** После «Выйти» переход на `/pets/<Мурка>` уводит на `/login?next=…` ([signed-out-1440](medical-record-web-01/signed-out-1440.png)), медкарты нет. Запрос API из этой страницы → 401, запрос без токена → 401. До выхода тот же запрос владельца → 200 (`apiOwnerA`). Юнит: 401/404 сбрасывают данные (`record-load.test.ts`). |
| MW-01.3 Мурка — данные сервера, Бобик — «из анкеты», без фиктивных дат | PASS | **Мурка** ([murka-1440](medical-record-web-01/murka-1440.png), [murka-390-full](medical-record-web-01/murka-390-full.png)): сроки «Просрочено на 14 дней · 12 сентября», «Через 7 дней · 3 октября», «Через 9 дней · 5 октября», «Ещё 2 срока»; прививки 12 марта 2026 · Айболит; график 4,5 → 4,4 → 4,2 и «−0,3 кг за 6 месяцев»; «Фортифлора — Завершён · 2–15 августа»; проверка 1 августа «Наблюдаем». **Бобик** ([bobik-1440](medical-record-web-01/bobik-1440.png), [bobik-390-full](medical-record-web-01/bobik-390-full.png)): «28 кг · Из анкеты, дата не указана»; «В анкете: привит(а) · Даты и препараты не указаны»; блоков «Сроки» и «Важно знать» нет; нигде нет даты. Юнит `view-model.test.ts` проверяет обе медкарты, в том числе что у Бобика в тексте разделов нет года. |
| MW-01.4 На 390 и 1440 px нет горизонтального скролла; навигация и действия доступны с клавиатуры | PASS | `scrollWidth > innerWidth` ложно для Мурки и Бобика на 1440 и 390, для ошибки на 390, а также на 1150, 1024, 900, 768, 760, 360, 320 (`overflowByWidth`). Клавиатура (`tabOrder1440`, `tabOrder390`): Tab проходит «Перейти к содержимому» → навигацию → «Все питомцы» → «Анкета» → «Изменить в анкете» → карточку проверки → нижнее меню. Невидимых остановок нет. Фокус при ошибке переходит на заголовок (`focused: health-problem-title`). Действий медкарты на MW-01 нет, они скрыты флагом (`hiddenActions` — все false). |
| MW-01.5 Ошибка запроса даёт повтор, а не пустую «здоровую» медкарту | PASS | **Первая загрузка.** Ответ 500 → «Не удалось загрузить медкарту» и «Повторить», медкарта не отрисована ([load-error-1440](medical-record-web-01/load-error-1440.png), [load-error-390](medical-record-web-01/load-error-390.png)). После снятия ошибки «Повторить» загружает медкарту (`retryRecovers`). **Обновление.** Переход анкета → медкарта при обрыве сети оставляет прежние данные и показывает баннер «Не удалось обновить медкарту. Показаны ранее загруженные данные. Повторить» ([stale-1440](medical-record-web-01/stale-1440.png)). «Повторить» убирает баннер. Кнопка занята только на время запроса. Если не загрузилась только история проверок, медкарта остаётся, у истории своё «Повторить». Юнит: `record-load.test.ts`. |

## Скриншоты

Все файлы — в [`medical-record-web-01/`](medical-record-web-01/). На `-390-full` нижнее меню перенесено в конец страницы только для экспорта, как в `-390-full` макета.

| Экран | 1440 | 390 |
|---|---|---|
| Мурка | [murka-1440](medical-record-web-01/murka-1440.png) | [видимая область](medical-record-web-01/murka-390.png), [вся страница](medical-record-web-01/murka-390-full.png) |
| Бобик | [bobik-1440](medical-record-web-01/bobik-1440.png) | [видимая область](medical-record-web-01/bobik-390.png), [вся страница](medical-record-web-01/bobik-390-full.png) |
| Загрузка | [loading-1440](medical-record-web-01/loading-1440.png) | — |
| Ошибка первой загрузки | [load-error-1440](medical-record-web-01/load-error-1440.png) | [load-error-390](medical-record-web-01/load-error-390.png) |
| Ошибка обновления | [stale-1440](medical-record-web-01/stale-1440.png) | — |
| Список питомцев | [pets-1440](medical-record-web-01/pets-1440.png) | — |
| Чужой питомец (B) | [foreign-1440](medical-record-web-01/foreign-1440.png) | — |
| После выхода | [signed-out-1440](medical-record-web-01/signed-out-1440.png) | — |

## Тесты

| Команда | Результат |
|---|---|
| `npm run typecheck` | OK: mobile, web, contracts, shared |
| `npm run lint` | OK, 0 замечаний |
| `npm test` (web, vitest) | 36 файлов, 265 тестов — все прошли; из них новые `tests/unit/features/medical-record/*`: 27 тестов |
| `npm test --workspace @lapka/shared` | 5 файлов, 34 теста — все прошли; новый `record-overview.test.ts`: 13 тестов |
| `npm test --workspace @lapka/mobile` | 41 файл, 347 тестов — все прошли (пакет `shared` изменился, мобильное не сломано) |
| `npm run test:integration` | 34 из 35 файлов, 342 из 344 тестов. Падает только `schema.test.ts` (2 теста): в локальной базе есть таблица `personal_data_consents` и функция `pd_consent_version_is_current`. Их миграция — из другой ветки (коммиты «Consent: …»), её нет в этой ветке. К MW-01 не относится, код ветки миграций не добавляет. |
| `node apps/web/scripts/seed-medical-record-demo.mjs` | Мурка и Бобик созданы (после `test:integration` фикстуры пересоздавались, `tests/integration/fixtures.test.ts`) |
| `PLAYWRIGHT=… CHROME=… node docs/verification/medical-record-web-01/verify.mjs` | exit 0, результат — `verify-output.json` |

Новые проверки:
- `record-overview.test.ts` (shared) — календарные дни, сроки (просрочено / скоро / позже, «с даты» после двух месяцев), порядок сроков, тренд веса без недатированной точки, текущие и завершённые курсы, `localToday`.
- `view-model.test.ts` (web) — медкарты Мурки и Бобика из макетных данных:
  - «привит» / «не привит» / «не указано»;
  - лекарства из анкеты;
  - диапазоны дат;
  - геометрия графика, в том числе без деления на ноль.
- `record-load.test.ts` (web):
  - переходы состояний и разбор ошибок API;
  - частичная ошибка истории;
  - флаги этапов.

Серверная логика не менялась. Доступ к `GET /pets/{id}/health` уже покрыт `tests/integration/medical-record-01.test.ts`, он в зелёной части прогона.

## Дефекты и непроверенное

1. **HTTP-статус чужого питомца — 200, а не 404.** Страница 404 показывается, данных нет. Но `pets/loading.tsx` начинает поток до `notFound()`, и Next.js 16 после этого не меняет статус (документация `loading.md`, «Status Codes»: в поток добавляется `noindex`). Так же ведёт себя и существующая `/pets/[id]/edit`. API отвечает честным 404. Исправление, если статус нужен: проверка в `proxy.ts` или отдельный `loading` без потока.
2. **Экран «Что добавить?» (`add`) не создан.** На MW-01 нет ни одной готовой формы, и выбор из нуля пунктов был бы пустым экраном. Маршрут `medicalRecordHref.add` и `addableRecordTypes()` готовы, экран добавит MW-02 вместе с первой формой (вес).
3. **Баннер «Начнём с заботы» для пустой медкарты** (7.3 спецификации, макет `medical-empty`) не показывается. Его единственное действие — «Добавить прививку», а оно появится на MW-03. Без действия баннер звал бы к тому, чего нельзя сделать. Нужно сделать на MW-03.
4. **«История проверок» без «Все».** Отдельной страницы истории питомца (`pet-history`) ни в одном этапе нет. Общая `/checks` не фильтрует по питомцу. В карточке — три последние проверки, вся история — через «История» в навигации.
5. **Закреплённые действия над нижним меню (≤760 px) не проверены вживую:** на MW-01 действий нет. CSS готов, проверить на MW-02.
6. **Не проверялись:** экранный диктор (VoiceOver/NVDA), шрифт 200%, 360×640 «шапка и сроки без скролла» (§10) и реальный iPhone Safari. Ширины 360 и 320 проверены только на горизонтальный скролл.
7. **Сид удаляет фикстурную Мурку** (`11111111-…-0001`) через API, чтобы в списке было одно имя. Следующий `test:integration` создаёт её заново. Фикстурный Рекс остаётся в списке владельца A.
8. **`verify.mjs` не входит в CI.** Ему нужны Playwright и Chrome вне репозитория (переменные `PLAYWRIGHT`, `CHROME`), запущенный сайт и демо-данные.
