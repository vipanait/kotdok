# Этап 0.1 — монорепозиторий и независимые сборки

Статус: **IN_PROGRESS**. Семь пунктов из девяти приняты; 0.1/05 подтверждён на GitHub частично, 0.1/06 закрыт — preview собирается, открывается и работает против staging. Этап не закрыт.

Исполнимый план: [stage-0.1-monorepo.md](../superpowers/plans/stage-0.1-monorepo.md).

## Что сделано

- Next.js перенесён в `apps/web` через `git mv`; поведение сайта не менялось.
- Настроены npm workspaces с единым корневым lockfile; `npm ci` воспроизводится побайтово.
- Созданы каркасы `packages/contracts` и `packages/shared`; веб берёт из shared список локалей, поэтому пакет проверяется и production-сборкой, а не только тестами.
- Создано минимальное Expo-приложение `apps/mobile`, экспортирующее JS для iOS и Android.
- Проверки и сборочные настройки разделены по приложениям; корневой `.gitignore` покрывает вложенные приложения.
- CI разделён на группы web и mobile с проверяемой логикой маршрутизации.
- Обновлены README и AGENTS.md.

## Матрица приёмки

| ID | Статус | Фактический результат |
| --- | --- | --- |
| 0.1/01 | PASS | Из `apps/web` проходят lint, typecheck, 65 unit-тестов, 13 интеграционных и production-сборка — все exit 0. Число исходных тестов сохранено (62 + 3 новых на разрешение workspace-пакетов). В `apps/mobile/dist` нет серверных ключей и ref рабочего проекта; серверные SDK отсутствуют в графе зависимостей мобильного приложения. Ручной сценарий preview-сайта — NOT RUN |
| 0.1/02 | PASS | `npm ci` в чистой копии exit 0, lockfile не изменился побайтово. Все workspace-пакеты разрешаются локально через симлинки. Полная пересборка lockfile обновляла 166 пакетов, поэтому он собран инкрементально: ни одна прямая зависимость веба не изменилась, изменились 22 транзитивных patch/minor-пакета, поднятые диапазонами Expo |
| 0.1/03 | PASS | Минимальный экран собирается в JS-бандл для обеих платформ; импорт из `@lapka/contracts` и `@lapka/shared` проходит в обоих приложениях. Запуск на реальных устройствах — этап 4 |
| 0.1/04 | PASS | Каждая команда workspace проходит отдельно; веб-сборка выполнена без Xcode, Android SDK и запущенного Metro. В графе мобильного приложения одна копия React 19.2.4 и одна копия react-native 0.86.3 — подтверждено `npm ls` и поиском физических каталогов |
| 0.1/05 | PARTIAL | Workflow разделён на группы `web` и `mobile`, правила маршрутизации покрыты 9 тестами. На PR #7 маршрутизация отработала на самом GitHub: `changes` включил обе группы, обе прошли. Что важнее, механизм доказал полезность — первый прогон **завалил** сборку на реальном дефекте (импорт сервиса анализа падал без ключа OpenAI), то есть падающая проверка действительно не даёт зелёного статуса. Не проверены четыре отдельных пробных изменения, показывающих, что изменение только в вебе не запускает мобильную группу и наоборот |
| 0.1/06 | PASS | Хостинг подтверждён: Vercel, проект `kotdok`, Root Directory `.`. Первый preview после переноса упал — Vercel искал `.next` в корне репозитория. Добавлен корневой `vercel.json` с install-командой, build-командой и `outputDirectory: apps/web/.next`; следующий preview собрался (`kotdok-2bjzr7y6r`, Ready, 1 мин). Preview переведён на тестовый проект Supabase и проверен владельцем в браузере: страницы отдаются, вход через форму работает против staging. После слияния PR #7 production выкачен и проверен смоуком |
| 0.1/07 | PASS | README описывает структуру, команды каждого приложения, локальный стенд и правила версий; все упомянутые пути существуют. AGENTS.md сохраняет управляемый блок правил Next.js, явно относит его к `apps/web` и добавляет правила монорепозитория; `node_modules/next/dist/docs/` разрешается из корня |
| 0.1/08 | PASS | `git check-ignore` подтверждает игнорирование `apps/web/.next`, `apps/web/node_modules`, `node_modules`, `apps/web/coverage`, `apps/web/.env.local`, `apps/mobile/.expo`, `apps/mobile/android`, `apps/mobile/ios`, `apps/mobile/node_modules`, `apps/mobile/dist` при фактически существующих файлах. `git status --untracked-files=all` после сборок и экспортов чист. Сохранены только `apps/web/.env.test` и `apps/web/.env.integration` без секретов |
| 0.1/09 | PASS | Конфигурации TypeScript и ESLint находятся внутри `apps/web` и не видят `apps/mobile`; корневого общего конфига нет. Мобильное приложение использует `expo/tsconfig.base` и штатный Metro — обходов разрешения модулей не добавлялось |

## Решения и отклонения от плана

1. **React Native зафиксирован на 0.86.3, а не на последней 0.87.1.** Expo SDK 57 бандлит 0.86.3; в 0.87 перемещены файлы, которые требует `@expo/metro-config`, и экспорт падал. React 19.2.4 удовлетворяет и Next.js, и React Native — одна версия на весь репозиторий не навязывалась, совместимость проверена.
2. **Корневой `overrides` для react-native.** `@expo/cli` объявляет его необязательным peer с диапазоном `*`, из-за чего npm поднимал в корень вторую копию.
3. **Lockfile собран инкрементально.** Полное разрешение с `overrides` обновляло 166 пакетов, включая Supabase, OpenAI, Vitest и ESLint-тулчейн. Это запрещено пунктом 0.1/02, поэтому lockfile восстановлен из коммита переноса и дополнен только новыми мобильными зависимостями.
4. **Логика маршрутизации CI вынесена из YAML** в тестируемый скрипт, чтобы её можно было менять без наблюдения за прогонами GitHub. Это дополнение к плану, а не замена приёмке 0.1/05.
5. **Интеграционные тесты подключены к CI** через `supabase/setup-cli` и `supabase start`. Прогон состоялся на PR #7: шаги подъёма Supabase, применения миграций на чистой БД и самих тестов прошли успешно.

## Известные ограничения

- Версия Node зафиксирована на 24.x в `engines.node` и `.node-version`; её же использует CI, на ней работает Vercel. Node 25 на хостинге недоступен.
- Экспорт JS не подтверждает нативную сборку. Подписанные development builds и запуск на реальных устройствах — этап 4.
- `apps/mobile` содержит экран-заглушку; авторизация и продуктовые экраны начинаются с этапа 4.

## История проверок

- [Запуск 20260906-070333Z](runs/stage-0.1/20260906-070333Z.md) — перенос, пакеты, Expo, CI.
- [Запуск 20260906-072750Z](runs/stage-0.1/20260906-072750Z.md) — push, Vercel, окружения; запись общая с этапом 0.
