# Этап 8/05 — обработчик удаления аккаунта 20260917-095100Z

Код: `main` на `a1caed4` (PR #36). План:
[stage-8.05-deletion-worker.md](../../../superpowers/plans/stage-8.05-deletion-worker.md), спека:
[2026-09-17-stage-8-05-deletion-worker-design.md](../../../superpowers/specs/2026-09-17-stage-8-05-deletion-worker-design.md).

## Развёртывание

| Шаг | Как | Результат |
| --- | --- | --- |
| Миграция на staging | `supabase db push --linked` (`rclnsbivyulqmvujiopv`) | `20260917120000` local = remote; шесть функций на месте |
| Миграция на production | SQL миграции и запись в `supabase_migrations.schema_migrations` одной транзакцией — обычный `db push` отказался бы из-за `20260910090000_check_job_queue` (2.16), её не трогали | 5 новых функций, 2 колонки, история `20260910070000, 20260910090000, 20260917120000`; `anon` не может вызвать `delete_account_data`; заявок на удаление на production — 0 |
| `CRON_SECRET` | Сгенерирован `openssl rand -hex 32` прямо в буфер обмена, вписан владельцем в Vercel (Production, Sensitive) | — |
| Мердж и выкатка | PR #36, CI зелёный, Vercel `success` для `a1caed4` | — |
| Крон без секрета | `GET https://lapka.my/api/cron/deletion-jobs` | `401` |
| Крон из Vercel | Владелец нажал Run в Settings → Cron Jobs; журнал Vercel | `200` |

## Сквозная проверка на staging

Через локальный `npm run dev:staging` (тот же код, что на production) против staging-Supabase.
Тестовый аккаунт `stage8-05-…@example.test` (`2fab1e89-ab7a-43b7-929b-9f8f37ec1925`) создан служебным
ключом, ему добавлены питомец, проверка и запись баланса; сессия получена одноразовой ссылкой, без пароля.

| Шаг | Результат |
| --- | --- |
| До | пользователь Auth 1, профиль 1, питомцы 1, проверки 1, записи баланса 1, архив 0 |
| `POST /api/v1/auth/reauth` | `200` |
| `POST /api/v1/account-deletion` | `202 {"status":"accepted"}` |
| `GET /api/v1/account-deletion/status` по квитанции | `completed` через **2,8 с** |
| Задача | `completed`, `attempts` 0, `error_code` пусто, `progress` — `data` 09:51:33.61, `auth` 09:51:33.92, `retain_until` +60 дней |
| После | пользователь Auth 0, профиль 0, питомцы 0, проверки 0, записи баланса 0, **архив 1** |
| Старый access token на `/api/v1/me` | `401` |

## Автоматические проверки (ветка перед мерджем, `cd77b98`)

| Набор | Результат |
| --- | --- |
| Модульные `npm run test --workspace @lapka/web` | 137 / 0 |
| Интеграционные на локальном Supabase | 189 / 0 |
| typecheck, lint, build | без ошибок |
| CI PR #36 | `web`, `audit`, `changes`, Vercel — pass |
