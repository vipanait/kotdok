# Прогон 6/01–6/03 на симуляторе, 23 сентября 2026, 13:04–13:13 UTC

- **Ревизия:** `d4f0f52` (ветка `feature/photo-uploads`), затем тестовая правка `b21cdf3`.
- **Окружение:** полностью локальное. Supabase CLI 2.115.0 (`supabase start`, Storage включён); API — `next dev -p 3100` с переменными из `apps/web/.env.integration` и настоящим ключом OpenAI из `apps/web/.env.staging.local` (запись в staging не производилась); Metro с `EXPO_PUBLIC_API_URL=http://127.0.0.1:3100`, `EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321`. `apps/mobile/.env.local` на время прогона переименован и возвращён.
- **Сборка:** `npx expo prebuild --platform ios --no-install && npx expo run:ios --device "iPhone 17e"` — новая dev-сборка (новые нативные модули `expo-image-picker`, `expo-image-manipulator`, `expo-file-system`). iOS 26.5, iPhone 17e (симулятор).
- **Аккаунт:** фикстура `owner-a@fixture.local` (5 проверок, питомцы Мурка и Рекс). Вход — одноразовой ссылкой `lapka://auth/callback?token_hash=…&type=magiclink`, выданной `POST /auth/v1/admin/generate_link` локального Supabase; пароль не вводился.
- **Исполнитель:** Claude (Opus 5.5), по поручению владельца.

## Сценарии

| # | Действия | Ожидалось | Получилось | Статус |
| --- | --- | --- | --- | --- |
| 1 | Шаг 1 формы: блок «Фото — необязательно» | Плитка 72×72 с пунктиром и камерой, подпись про удаление | Так и есть | PASS |
| 2 | Плитка → «Из галереи» | Системный выбор фото | **Первый раз:** запрос полного доступа ко всей медиатеке — лишний. Исправлено (`d4f0f52`): галерея больше не просит разрешения, системный выбор отдаёт только выбранные фото. **После правки:** выбор открылся без запроса, заголовок «Select up to 3 photos» | FAIL → исправлено → PASS |
| 3 | Отказ в доступе к медиатеке («Don't Allow», до правки) | Форма работает, без фото | Форма осталась рабочей, ошибки нет | PASS |
| 4 | Выбрать 3 фото, затем попытаться выбрать 4-е | После 3-го плитка добавления исчезает; система не даёт больше 3 | Три превью с крестиками, плитки нет; четвёртое система не выделяет | PASS |
| 5 | Крестик на 3-м фото | Фото убрано, плитка вернулась | Так и есть | PASS |
| 6 | Шаг 2 | Итог показывает число фото | «Фото: 2» | PASS |
| 7 | «Проверить» с 2 фото | Загрузка, анализ, файлы удалены, списана одна проверка | Хранилище (опрос раз в 0.5 с): выдано 2 загрузки → 2 объекта **250 711 и 543 304 байт**, `attached=2` → 0 объектов, 0 строк. `POST /uploads` 201, `POST /checks` 202 за 15.9 с, задача `completed`, `photo_count=2`, кредиты 5 → 4 | PASS |
| 8 | Экран результата после сценария 7 | Результат открывается сам | **Не открылся** — ожидание осталось на экране. Причина не в фото: во время прогона файл экрана менялся, Fast Refresh выполнил очистку эффекта `onScreen.current = false`, а эффект его обратно не поднимает, и цикл ожидания тихо вышел. Существующий код, только dev-режим. Результат есть в истории | см. «Ошибки» |
| 9 | Результат из истории | Раздел о фото | **Раздела не было**: мобильный экран не показывал `full_response.photo_observations`. Исправлено (`d4f0f52`): раздел «Что видно на фото». После правки: «На фото кошка не видна, оценить внешние признаки по изображению невозможно.» (в симуляторе только пейзажи — ответ верный) | FAIL → исправлено → PASS |
| 10 | Остановить контейнер Storage, отправить проверку с 1 фото | Понятная ошибка, проверка не списана, ничего не осталось | `POST /uploads` 503 через 29.8 с (`grant_sign_failed`), на экране «Сервис временно недоступен», кредиты 4, строк и объектов 0 | PASS |
| 11 | Запустить Storage, «Попробовать ещё раз» | Проверка проходит с тем же фото | Задача `completed` за 9 с, результат открылся сам, раздел «Что видно на фото» на месте, кредиты 4 → 3, хранилище пустое | PASS |

## Не проверено в этом прогоне

- «Снять фото»: в симуляторе нет камеры.
- Удаление EXIF (геометки) при перекодировании в JPEG: объекты удаляются сразу после анализа, их содержимое не сохранялось.
- Staging и hosted Storage: миграция не применялась без согласия владельца.
