# Повторный прогон 6/01–6/03 после правок ревью, 23 сентября 2026, 13:43–13:49 UTC

- **Ревизия:** `e12e2e3` (PR #48). Нативная сборка та же, что в [первом прогоне](20260923-130400Z.md): менялся только JS.
- **Окружение:** как в первом прогоне — локальный Supabase, `next dev -p 3100` с настоящим ключом OpenAI, Metro `expo start --dev-client`. Язык аккаунта после интеграционных тестов — английский; питомец и баланс заведены прямо в базе.
- **Вход:** одноразовая ссылка `magiclink` локального Supabase. Первая ссылка не сработала (приложение ещё показывало «The session has expired»), вторая — да.

| # | Действия | Ожидалось | Получилось | Статус |
| --- | --- | --- | --- | --- |
| 1 | Форма на английском | Строки блока фото переведены | «Photos — optional», «Up to three photos…», «Add a photo / Take a photo / From library» | PASS |
| 2 | 3 фото из галереи, отправка | Задача создаётся до загрузки фото; фото удаляются после анализа | Во время анализа: 3 объекта (391 292, 372 838, 752 420 байт), задача `processing`; после — 0 объектов, 0 строк, `completed` за 12 с, кредиты 5 → 4 | PASS |
| 3 | Экран результата | Открывается сам, раздел о фото | «What’s visible in the photos» с описанием всех трёх снимков | PASS |
| 4 | Баланс 0, отправка с 1 фото | Отказ `insufficient_credits`, фото удалены | Задача `failed` / `insufficient_credits`, 0 объектов, на экране «Not enough checks on your balance» | PASS |
| 5 | Вернуть баланс, «Try again» | Новый ключ, успех с первого повтора | Новый `Idempotency-Key`, задача `completed` за 10 с, кредиты 3 → 2, хранилище пустое, результат открылся сам | PASS |

Замечено попутно, не из этой ветки: при `insufficient_credits`, пришедшем прямо в ответ на `POST /checks`, экран предлагает «Try again», а не «Запросить проверку» — ответ приходит как `ApiError`, а кнопку выбирает `AppError.kind`.
