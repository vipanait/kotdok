# Открытые вопросы

Всё, что осталось нерешённым или непроверенным после этапов 0, 0.1, 1 и 2. Обновляется вместе с отчётами этапов. Пункт считается закрытым, только когда он зачёркнут и помечен «сделано».

Состояние на 6 сентября 2026, ревизия ветки `stage-0-baseline`. Решённые пункты оставлены зачёркнутыми, чтобы был виден путь.

## 1. Требуют решения или доступа владельца

| # | Вопрос | Что блокирует | Почему нельзя решить без владельца |
| --- | --- | --- | --- |
| 1.1 | ~~Отдельный тестовый проект Supabase~~ — **сделано** | — | Создан `lapka-staging` (`rclnsbivyulqmvujiopv`), $0/мес, схема идентична production, история миграций совпадает. Публичные значения в `apps/web/.env.test` |
| 1.2 | Preview-деплои Vercel работают с боевыми ключами | Приёмку 0/04 | Тестовый проект теперь есть, но переключить Preview на него нельзя без его service-role-ключа: Management API секретные ключи не отдаёт, значение берётся из дашборда Supabase. Плюс само изменение — настройка проекта хостинга |
| 1.3 | Оба ключа помечены в Vercel как Non-sensitive | То же | Их значения читаются через настройки проекта и API. Смена типа переменной — действие в аккаунте |
| 1.4 | Чем запускать worker чаще раза в сутки | **Этап 6 целиком** | Лимиты выяснены: тариф Hobby, выполнение до 300 с — этого достаточно. Планировщика нет: cron на Hobby допускает один запуск в сутки с точностью ±59 минут. Выбрать между Pro, `pg_cron` в Supabase и внешним планировщиком может только владелец |
| 1.12 | **Загрузка фото уже не работает в production** | Этап 6 и текущий продукт | Приложение принимает до 25 МБ в одном запросе, Vercel обрывает всё свыше 4.5 МБ. Проверено на `lapka.my`: 1 МБ → 403 от приложения, 5 МБ → 413 от платформы. Лечится переносом загрузки напрямую в хранилище (этап 6); до этого пользователь с парой обычных фотографий получает ошибку без объяснения |
| 1.5 | Настройки резервного копирования Supabase и срок хранения копий | Пункт 8/09 | Требуется доступ к настройкам проекта |
| 1.6 | Срок хранения фотографий и TTL брошенных загрузок | Этап 6 | Решение о приватности продукта |
| 1.7 | Обязательства по хранению финансовых записей, юрисдикция | Окончательную очистку на этапе 8 | План прямо запрещает придумывать срок хранения без определения обязательств |
| 1.8 | Опубликованный срок выполнения удаления аккаунта | Пункты 9/05, 9/06 | Измеряется пробными удалениями на staging; сам процесс удаления появится на этапе 8 |
| 1.9 | Обновление уязвимых зависимостей | Публикацию | 21 уязвимость (7 high), среди них DoS в Next.js Server Components, sharp/libvips, ws, postcss. План требует отдельного изменения безопасности до публикации, а не попутного обновления |
| 1.10 | Доступы: Apple Developer и App Store Connect, Google Play Console, Expo/EAS, OAuth Google и Яндекса, DNS и SMTP | Этапы 4, 5, 11 | Регистрация, 2FA и оплата — действия владельца |
| 1.11 | Реальные iPhone и Android для ручных сценариев | Этапы 4–7, 9, 10 | Симулятор не заменяет проверки камеры, OAuth, ссылок возврата и поведения при сворачивании |

## 2. Непроверенное в уже сделанной работе

| # | Что не проверено | Пункт | Почему |
| --- | --- | --- | --- |
| 2.1 | Ручной сценарий сайта: вход → питомец → анализ → история → выход, а также сверка «создать через API — увидеть на сайте» | 0, 0.1, 1, 2, 3 | Ни разу не пройден. Preview закрыт Vercel Deployment Protection, расширение браузера в этой среде не подключено |
| 2.2 | HTTP-проверка сайта после переноса в `apps/web` | 0.1/06 | Preview собирается (статус Ready), но все пути отдают страницу входа Vercel |
| 2.3 | Маршрутизация CI на самом GitHub | 0.1/05 | Ветка отправлена, но workflow срабатывает на `push` только в `main` и на `pull_request`; PR по решению владельца не создавался. Локально проверена только логика — 9 тестов |
| 2.4 | Пробная задача worker | 0/06 | Зависит от 1.4 |
| 2.5 | ~~Матрица A/B/admin по каждому ресурсу~~ — **сделано** | 2/02 | Закрыта на этапе 3: чужой питомец и чужая проверка дают 404 на чтение, изменение и удаление; административный шлюз проверен отдельно |
| 2.6 | Storage-политики | 2/04 | Buckets пока не используются; появятся на этапе 6 вместе с загрузками фото |
| 2.7 | Получение токена настоящим входом на **тестовом сайте** и вызов `/me` | 2/01 | Тестовый проект появился, но на него ещё не указывает ни один развёрнутый сайт: Preview использует production (1.2). Локальный эквивалент выполнен на реальном Supabase |

## 3. Технические вопросы без внешней зависимости

| # | Вопрос | Комментарий |
| --- | --- | --- |
| 3.1 | ~~Версия Node не согласована~~ — **сделано** | Зафиксирована 24.x в `engines.node` и `.node-version`; CI переведён на неё, Vercel уже на ней. Node 25 недоступен: Vercel предлагает только 24.x, 22.x и 20.x |
| 3.2 | Ветка `stage-0-baseline` не влита | Pull request не создавался; `lapka.my` продолжает отдавать сборку из `main`. Схема БД уже обновлена, поэтому выкат ветки больше ничем не блокирован |
| 3.3 | Оставшиеся предупреждения Supabase security advisors | Изменяемый `search_path` у `search_vet_knowledge`; расширение `vector` установлено в схеме `public`; отключена защита от скомпрометированных паролей в Auth. Первые два — гигиена, третье меняет поведение регистрации и требует решения. Замечание про `handle_new_user()` закрыто |
| 3.4 | Инструмент мобильных E2E не выбран | План предписывает выбирать его после прототипа, под возможности CI. Этап 7 |
| 3.5 | Интеграционные тесты в CI не проверялись в реальном прогоне | Шаги `supabase/setup-cli` и `supabase start` в workflow добавлены, но ни разу не выполнялись на GitHub. Зависит от 2.3 |
| 3.6 | ~~`.env.test` содержит заглушку~~ — **сделано** | Указывает на `lapka-staging`; service-role и AI-ключи остались заглушками и в репозиторий не коммитятся |

## 4. Решения, принятые за владельца

Их стоит подтвердить или отменить; каждое зафиксировано в отчёте соответствующего этапа.

| # | Решение | Где |
| --- | --- | --- |
| 4.1 | React Native закреплена на 0.86.3 | Подтверждено: `expo-doctor` проходит 21/21, версию не помечает — это и есть последняя, которую поддерживает Expo SDK 57. React оставлен на 19.2.4 ради общей копии с Next.js и внесён в `expo.install.exclude` |
| 4.2 | Lockfile собран инкрементально, без обновления 166 пакетов | Этап 0.1. Полное разрешение обновило бы Supabase, OpenAI, Vitest и ESLint-тулчейн — то, что план запрещает совмещать с переносом |
| 4.3 | Маршрут анализа теперь проверяет запрос до чтения аккаунта | Этап 1. Неверное тело при нулевом балансе даёт 400, а не 402 |
| 4.4 | Числовые лимиты частоты: анализ 10/час, дополнительная проверка 5/час, обратная связь 10/сутки, статус удаления 60/час | Этап 2 |
| 4.5 | Настройка Vercel вынесена в `vercel.json` в репозитории, а не в параметры проекта | Этап 0.1 |
| 4.6 | TypeScript поднят с 5.9.3 до 6.0.3 | Этого требует Expo SDK 57; после обновления typecheck всех пакетов, lint, 113 тестов и сборка веба проходят. Последняя версия TypeScript — 7.0.2, но её Expo не запрашивает |
| 4.7 | Node зафиксирован на 24.x, а не на локальной 25.2.1 | Vercel предлагает только 24.x, 22.x и 20.x; Node 25 — Current-релиз и на хостинге недоступен |
