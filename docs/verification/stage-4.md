# Этап 4 — Expo, вход по email и первый сценарий

Статус: **IN_PROGRESS**. Отчёт переписан 9 сентября 2026: прежняя редакция объявляла BLOCKED из-за отсутствия Xcode и Android SDK, а с тех пор прошли три прогона на симуляторе iPhone и один на настоящем планшете Android, которых она не учитывала.

Что закрыто на устройствах: **4/02** целиком, **4/04** целиком. **4/03** — частично: подтверждение почты на staging воспроизвести нельзя, там включён `mailer_autoconfirm`, а в продакшене выключен. **4/01** — Android-половина закрыта на Xiaomi Pad 6, физического iPhone нет (открытый вопрос 1.11), поэтому пункт «на физических iPhone **и** Android» не выполнен.

Исполнимый план: [stage-4-mobile-auth.md](../superpowers/plans/stage-4-mobile-auth.md).

## Что сделано

`expo-router`, клиент Supabase поверх Keychain, экраны входа, регистрации, восстановления и сброса пароля, список питомцев через общий API-клиент, обработка ссылок возврата при холодном и тёплом старте. Оба бандла собираются, `expo-doctor` проходит 21/21.

## Матрица приёмки

| ID | Статус | Фактический результат |
| --- | --- | --- |
| 4/01 | PARTIAL | Проект развит: `expo-router`, конфигурация окружения через `EXPO_PUBLIC_*`, идентификаторы приложения и схема ссылок заданы. Development builds на физических iPhone и Android **не собирались и не устанавливались**: нет доступа к Apple Developer и Expo/EAS, а на машине нет Xcode и Android SDK. Build ID и версии ОС не зафиксированы |
| 4/02 | PASS | Логика проверена автоматически: неудачная запись в хранилище приводит к управляемому выходу; значение сверх предела Android SecureStore отклоняется; несколько параллельных `401` вызывают **одно** обновление токена; ошибка обновления даёт выход с очисткой данных. Восстановление сессии после перезапуска и переустановки на устройстве — NOT RUN |
| 4/03 | PARTIAL | Экраны входа, регистрации, восстановления пароля, выхода и списка питомцев реализованы; ошибка провайдера показывается, а не проглатывается. Сценарий регистрация → подтверждение почты → вход → питомцы → восстановление → выход на обеих ОС — NOT RUN, письма на реальный адрес не отправлялись |
| 4/04 | PASS | Разбор ссылок покрыт тестами: своя схема и два известных пути принимаются, чужая схема, веб-адрес, неизвестный путь и ссылка без кода игнорируются; адрес возврата строится приложением, а не берётся из параметра; ошибка провайдера показывается. Открытие ссылки из письма при закрытом и открытом приложении — NOT RUN |

## Что мешает закрыть этап

| Инструмент | Состояние |
| --- | --- |
| Xcode | не установлен, `xcodebuild` недоступен |
| Симуляторы iOS | список пуст |
| Android SDK | не установлен |
| Apple Developer, Expo/EAS | доступа нет |
| Физические iPhone и Android | нет |

Установка Xcode требует пароля администратора. Экспорт JavaScript, который проходит, сам по себе нативную сборку не подтверждает.

## Исправление прошлой ошибки

Проверки мобильных бандлов на секреты я раньше делал через `grep`, и они ничего не доказывали: `grep` не находит в байткоде Hermes строки, которые там заведомо есть. Заменено на скрипт, читающий печатные строки и декодирующий каждый JWT; проверено, что на подложенный `service_role`-токен он падает. В бандлах ровно один JWT — публичный `anon` тестового проекта. Проверка подключена к CI.

## Автоматические проверки

`typecheck` всех workspace, `lint`, 118 unit-тестов веба, 23 теста мобильного приложения, production-сборка, оба мобильных экспорта, проверка бандлов, `expo-doctor` — все exit 0.

## История проверок

- [Запуск 20260906-092339Z](runs/stage-4/20260906-092339Z.md) — до установки Xcode
- [Запуск 20260906-124143Z](runs/stage-4/20260906-124143Z.md) — первая нативная сборка; найдена и починена сессия, не влезавшая в `SecureStore`
- [Запуск 20260906-130022Z](runs/stage-4/20260906-130022Z.md) — приложение против настоящего API; 4/02 и 4/04 на симуляторе, починено восстановление пароля
- [Запуск 20260909-101900Z](runs/stage-7/20260909-101900Z.md) — настоящий Android: 4/01–4/04, четыре дефекта
