# Этап 1 — контракт и отделение сервисов

Статус: **IN_PROGRESS**. Контракты, OpenAPI и модель состояния аккаунта готовы; разделение сервисов и перевод старых маршрутов в работе. Этап не закрыт.

Исполнимый план: [stage-1-contracts-and-services.md](../superpowers/plans/stage-1-contracts-and-services.md).

## Главное этого этапа

При работе над моделью состояния аккаунта найдена и исправлена **действующая уязвимость в production**: роли `anon` и `authenticated` имели полный DML на все таблицы, а политики четырёх таблиц были написаны `for all using (...)` без `with check`. Вошедший пользователь мог публичным ключом выставить себе любой баланс и роль `admin`. Воспроизведено на локальном стенде с идентичными грантами, исправлено миграцией, применено к production, сайт после этого проверен смоуком.

Подробности, вывод воспроизведения и состояние production после исправления — в [записи цикла проверки](runs/stage-1/20260906-075904Z.md).

## Матрица приёмки

| ID | Статус | Фактический результат |
| --- | --- | --- |
| 1/01 | PASS | `docs/api/openapi.yaml` порождается из контрактов и принимается валидатором `@readme/openapi-parser`. Все 12 путей раздела 3 дорожной карты описаны; тест проверяет, что у каждой операции есть security, успешный ответ и ответы с ошибками, и что публичных операций ровно две — health и статус удаления |
| 1/02 | PASS | `packages/contracts` — единственный источник схем; документ генерируется из них, повторная генерация не даёт diff. Проверено, что расхождение ловится: добавление поля `email` в схему профиля роняет сравнение документа и тест разрешённых ключей `/me`. Схема `/me` строгая и не пропускает `email`, `aud`, `app_metadata` и токены |
| 1/03 | PASS | `profiles.status` со значениями `active`/`deleting` и ограничением; общий guard `loadAccount` пропускает активный аккаунт и отклоняет `deleting` и отсутствующий. На реальной БД пользовательский токен не может сменить статус — прав на запись у него больше нет |
| 1/04 | NOT RUN | Документ модели задач, загрузок и удаления не написан |
| 1/05 | PARTIAL | Общие модели вынесены: `SymptomCheckRecord` переехал из UI-компонента в контракты, оба приложения импортируют пакет, typecheck отклоняет неверный запрос, runtime-валидация — неверный ответ. Типизированный API-клиент для Expo не создан |
| 1/06 | NOT RUN | Разделение HTTP-обработчика анализа и бизнес-сервиса не выполнено |
| 1/07 | NOT RUN | Старые веб-маршруты на общие сервисы не переведены |

## Автоматические проверки

`typecheck` (все workspace), `lint`, 89 unit-тестов, 26 интеграционных на реальном PostgreSQL, production-сборка — все exit 0.

## Ручные проверки

Смоук `lapka.my` после применения миграции к production: шесть публичных путей отвечают 200 и рендерят приложение. Полный ручной сценарий сайта (вход → питомец → анализ → история) — NOT RUN.

## История проверок

- [Запуск 20260906-075904Z](runs/stage-1/20260906-075904Z.md) — контракты, OpenAPI, модель состояния аккаунта, уязвимость и её исправление.
