# Отчёт этапа 5/01 — вход через Google и Яндекс в приложении

## Паспорт

| Поле | Значение |
| --- | --- |
| Этап | 5/01 — «Google и Яндекс: системный браузер, поддерживаемый Supabase поток с PKCE и безопасный возврат в приложение» |
| Статус | **PARTIAL** — обе ОС пройдены на реальных устройствах; открытым остаётся 1.19 (склейка по адресу) |
| Дата | 8 сентября 2026 |
| План | [stage-5.01-provider-sign-in.md](../superpowers/plans/stage-5.01-provider-sign-in.md), дизайн — [спека](../superpowers/specs/2026-09-08-stage-5-01-provider-sign-in-design.md) |
| Проверенный код | ветка `mobile/stage-5-provider-sign-in`, коммит `31c955c` |
| Окружение | Xcode 26.6, iPhone 17 Pro (iOS 26.5), Expo SDK 57, Supabase `rclnsbivyulqmvujiopv` |
| Запись проверки | [runs/stage-5/20260908-095906Z.md](runs/stage-5/20260908-095906Z.md) |

Остальные пункты этапа 5 (Apple 5/02–5/07, связывание 5/05, ротация секретов 5/09) в этот заход не входили: доступа к Apple Developer нет.

## Что сделано

| Файл | Что изменено | Зачем |
| --- | --- | --- |
| `apps/mobile/src/lib/auth-links.ts` | `PROVIDER_RETURN_URL` и `parseProviderReturn` | Провайдерский вход возвращается на собственный путь, которого не знает разбор ссылок из писем: на Android браузер отдаёт ссылку и системе, и общий путь позволил бы обменять код дважды |
| `apps/mobile/src/lib/provider-sign-in.ts` | Логика входа: три исхода и правила выбора | Отмена отличается от сбоя, чужой адрес не обменивается, слова провайдера не показываются пользователю |
| `apps/mobile/src/lib/webcrypto.ts` | Полифил `crypto.subtle.digest` и `getRandomValues` на `expo-crypto` | Без него PKCE деградировал до `plain`, а verifier строился из `Math.random` |
| `apps/mobile/src/providers/AuthProvider.tsx` | `signInWithProvider` поверх `expo-web-browser` | Системный браузер, PKCE-verifier от Supabase, обмен кода |
| `apps/mobile/src/features/auth/ProviderButtons.tsx` | Кнопки перестали быть декорацией | Спиннер на нажатой, блокировка второй, исход отдаётся экрану |
| `apps/mobile/src/features/auth/provider-notice.ts` | Решение о тексте и тоне сообщения | Одно на два экрана и покрыто тестом |
| `apps/mobile/app/sign-in.tsx`, `sign-up.tsx` | Баннер исхода | Сбой виден там же, где ошибки email-входа |

## Матрица приёмки

| ID | Критерий | Способ | Фактический результат | Статус |
| --- | --- | --- | --- | --- |
| 5/01 | Google пройден на реальном iOS | Ручной вход владельца на iPhone 13, 9 сентября | Пройден **на физическом устройстве**: `panait.vi@gmail.com`, алерт 20:36:47, коллбэк 20:36:51.760, запрос к API 20:36:52.366, возврат на «Питомцев». Адреса не было в базе — склейки нет. Прежний прогон на симуляторе: входы 09:32:57Z и 09:35:31Z | **PASS** |
| 5/01 | Яндекс пройден на реальном iOS | Ручной вход владельца на iPhone 13, 9 сентября | Пройден **на физическом устройстве**: `panait.v@yandex.ru`, алерт 20:40:31, коллбэк 20:41:19.555, запросы к API 20:41:20.065, возврат на «Питомцев». Вход прицепился к существующему email-аккаунту — 1.19. Прежний прогон на симуляторе: входы 09:52:05Z и 09:55:23Z | **PASS** (с оговоркой 1.19) |
| 5/01 | Google пройден на реальном Android | Ручной вход владельца на Xiaomi Pad 6, 9 сентября | Пройден: новый аккаунт `dessy.tjia@gmail.com`, создан 10:53:20, вход 10:53:21, провайдер `google`. Приложение вернулось на «Питомцев» | **PASS** |
| 5/01 | Яндекс пройден на реальном Android | Там же | Пройден: `panait.v@yandex.ru`, вход 10:51:57, identity `custom:yandex`. Приложение вернулось на «Питомцев» | **PASS** |
| 5/01 | Внешний браузер, не встроенный webview | Проверка активности на переднем плане | Обе кнопки открывают Chrome Custom Tab на `passport.yandex.ru` и `accounts.google.com` | **PASS** |
| 5/01 | Отмена возвращает экран входа | Закрытие браузера на экране провайдера | Экран входа, молча, кнопки снова активны | PASS |
| 5/01 | Поддерживаемый поток с PKCE | Лог приложения и исходник `auth-js` | SHA-256: при сломанном полифиле вход падал с `TypeError`, при отсутствующем писал `WebCrypto API is not supported`; в сессии после исправления вход прошёл без обоих признаков | PASS |
| 5/01 | Неверный state/PKCE и повтор кода отклоняются | Автоматические тесты с подменёнными браузером и клиентом | Чужой адрес возврата, ответ без кода, отказ провайдера и отклонённый обмен не создают сессию | PASS (на живом провайдере не воспроизводилось) |
| 5/01 | Сессия принадлежит выбранному аккаунту | Сверка в Supabase | Пользователь и провайдер совпадают с выбранными. Оговорка: вход Яндексом прицепился к существующему email-аккаунту с тем же адресом | PARTIAL |

## Решения и отклонения

1. **Отмена входа ничего не сообщает.** Нейтральное «Вход отменён» было сделано и убрано по решению владельца после показа на симуляторе.
2. **Staging зависит от production.** Userinfo `custom:yandex` указывает на `https://lapka.my/api/auth/yandex/userinfo`. Edge Function внутри staging-проекта написана и локально проверена, но отброшена. Записано как 1.17 в открытых вопросах.
3. **Автоматическое связывание аккаунтов оставлено включённым** — 4.9 в открытых вопросах, к вопросу предполагается вернуться в 5/05.
4. **Диагностика отказа провайдера** попадает в лог dev-сборки: без неё причина `Error getting user email from external provider` не была бы видна с телефона. Код обмена и токены в лог не пишутся.

## Автоматические проверки

| Набор | Команда | Exit | Результат |
| --- | --- | --- | --- |
| Тесты приложения | `npm run test --workspace @lapka/mobile` | 0 | 93 passed |
| Тесты сайта | `npm run test --workspace @lapka/web` | 0 | 119 passed |
| Тесты общего пакета | `npm run test --workspace @lapka/shared` | 0 | 5 passed |
| Типы | `npm run typecheck --workspaces --if-present` | 0 | — |
| Экспорт бандла | `npm run export:ios --workspace @lapka/mobile` | 0 | — |
| Секреты в бандле | `npm run verify:bundle --workspace @lapka/mobile` | 0 | серверного материала нет |
| Нативная сборка | `expo run:ios` | 0 | установлена и запущена на iPhone 17 Pro |

## Что мешает закрыть пункт полностью

| Препятствие | Состояние |
| --- | --- |
| Android SDK | не установлен, снят решением владельца; проверка входа на Android — 2.8 |
| Реальные iPhone и Android | **снято 9 сентября**: Android — Xiaomi Pad 6, iOS — iPhone 13 (iOS 26.6.1). Оба провайдера пройдены на живых устройствах |
| Склейка аккаунтов по адресу | открыто, 1.19: вход Яндексом прицепляется к email-аккаунту с тем же адресом. Пункт 5/05 такое запрещает; нужно решение владельца |

Чекбокс 5/01 в дорожной карте остаётся снятым, но причина сменилась. Требование
«обе ОС на реальных устройствах» выполнено. Осталось единственное препятствие —
нерешённый вопрос 1.19: критерий «сессия принадлежит выбранному аккаунту» по Яндексу
не закрыт, пока владелец не решит, считать склейку по адресу допустимым отступлением
или запретить её настройкой проекта.


## Дополнение 9 сентября: Android и найденный на нём дефект

Оба провайдера пройдены на настоящем планшете. Первый же живой вход показал то,
чего не было видно на iOS.

**Вход срабатывал, а человек оставался на «странице не найдена».** Яндекс в 10:46
завёл сессию, привязал identity, всё записалось в базу — и приложение осталось на
`+not-found`. Снаружи это неотличимо от неудачи, и естественная реакция — пробовать
снова, хотя вход уже состоялся. Подтверждено перезапуском: после него открылись
«Питомцы» с питомцем этого аккаунта.

Причина: в манифесте схема `lapka` заявлена без ограничения по хосту, поэтому
Android отдаёт адрес возврата и браузерному сеансу, и главной активности. Роутер
идёт по маршруту `auth/provider`, которого нет. На iOS `openAuthSessionAsync`
забирает адрес себе, и система его никуда не отправляет — оттого дефект и не
всплывал полгода.

Исправлено маршрутом, который намеренно ничего не делает: код он не читает —
обмен уже идёт, а повтор упёрся бы в потраченный код, — и передаёт управление
заставке. Проверено трижды: с сессией → «Питомцы», без сессии → экран входа,
живой вход Яндексом и Google → «Питомцы».

**Наблюдение к 5/05.** `panait.v@yandex.ru` теперь имеет провайдеров
`['email', 'custom:yandex']`: вход Яндексом привязался к уже существовавшему
email-аккаунту с тем же адресом. Это делает Supabase, а не наш код, но пункт 5/05
прямо запрещает объединять аккаунты сравнением адресов. Требуется решение
владельца, вынесено в открытые вопросы.

## Дополнение 9 сентября: реальный iPhone

Оба провайдера пройдены на **физическом iPhone 13 (iOS 26.6.1, сборка 23G83)** —
последнее, чего не хватало приёмке по устройствам. Запись прогона:
[runs/stage-5/20260909-123500Z.md](runs/stage-5/20260909-123500Z.md).

Ревизия `6ff4bcb`. Мобильный код на ней не отличается от проверявшегося ранее:
`git diff --stat server/stage-8-late-callbacks...HEAD -- apps/mobile packages` пуст.

Экран телефона агенту недоступен: инструмент симулятора физическое устройство не
показывает и не драйвит. Поэтому ход потока снимался из системного лога устройства
(`idevicesyslog`), а состояния экранов подтверждал владелец. В отчёте эти два вида
свидетельств разделены.

Хронология обоих входов однотипна и читается однозначно: системный алерт
`ASWebAuthenticationSession` с именем staging-хоста → нажатие → регистрация
`Callback { scheme: lapka }` → соединение с API в пределах секунды. Отмена
подтверждена отрицанием: коллбэк не регистрировался ни разу, обращений к API нет.

**Наблюдение: браузерная сессия переживает выход из приложения.**
`openAuthSessionAsync` вызывается без `preferEphemeralSession`, поэтому куки общие
с Safari. После «Выйти» следующий тап по кнопке провайдера завершает вход молча, за
четыре секунды и без формы. Это же сломало первый прогон отмены — он был засчитан
как отмена, хотя в логе виден повторный вход; проверка переделана после выхода из
аккаунта. Само поведение штатное, но на общем устройстве следующий человек попадёт
в предыдущий аккаунт, не вводя пароль.

**Что этот заход не закрывает.** Пункт 4/01: сборка на устройстве использует
staging только наполовину — Supabase staging, а `EXPO_PUBLIC_API_URL` смотрит на
локальный dev-сервер владельца. Адреса staging-деплоя в репозитории нет.
